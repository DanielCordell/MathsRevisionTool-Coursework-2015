#pragma once
// Returns an integer value between min and max inclusive, will be used for random question generators to generate numbers randomly.
int getRand(int min, int max);

// Returns a random boolean value.
bool getBool();

// Returns either +1 or -1, will be used to randomly turn some numbers negative.
int getPlMn();
